# 贡献指南

1. 先搜索现有 Issue，说明问题、期望行为和复现步骤。
2. 从 `main` 创建小范围分支，避免混入无关格式化。
3. 自研 C++ 使用 C++17、`Vp` 命名空间和 `vp_` 文件前缀。
4. 每个自研 `.h/.cpp` 不超过 800 个物理行，控制语句左大括号另起一行。
5. 新功能或修复必须包含核心逻辑、边界和错误路径测试。
6. 使用 `python sgraphBuildTools/vp_build.py --test` 验证 Release 构建和普通测试；
   位图或基准变更同时增加 `--benchmarks`，纯核心修改另跑 `--core --test`。
7. Pull Request 说明行为变化、测试证据和兼容性影响。

贡献自研代码即表示你有权提交该内容，并同意其按本仓库自研代码许可证提供。第三方文件不要
直接修改；升级依赖时应说明来源、版本、哈希和许可证。
